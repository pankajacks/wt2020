<?php

/**
 * Array in PHP
 * 
 * $arr = array("pink") creates a new array
 * 
 * array_pop($arr)
 * array_push($arr,"blue","yellow");
 * 
 * array_shift(array) Remove the first element and retrun it
 * array_unshift(array, value1, value2, ...) insert new element to the begining of array
 * 
 * array_splice(array, start, length, array)
 * The array_splice() function removes selected elements from an array and replaces 
 * it with new elements. The function also returns an array with the removed elements.
 * If the function does not remove any elements (length=0), 
 * the replaced array will be inserted from the position of the start parameter
 * 
 * Associative array
 * 
 * array_key_exists(key, array)
 * array_keys(array)
 * 
 * count($arr)/sizeof($arr)
 * 
 * 2d Array
 * 
 */

?>